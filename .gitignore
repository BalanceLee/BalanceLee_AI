# Runtime data
data/
chat_uploads/
*.db
*.db-shm
*.db-wal
*.db-journal
*.sqlite
*.sqlite3

# Local configuration and secrets
config.local.yaml
config.yaml.backup
*.backup
.env
.env.*
!.env.example
*.pem
*.key
*.crt
# NOTE: config.yaml is intentionally tracked (keys removed by user) so others can clone & run.

# Upgrade / settings backups
.upgrade-backup/

# Eino runtime task board (under skills_dir)
skills/.eino/

# Build outputs (binary file excluded by default; uncomment to track)
# cyberstrike-ai
# cyberstrike-ai.exe
# balancelee-ai
# balancelee-ai.exe
# server
# server.exe
# NOTE: balancelee-ai.exe and server.exe are intentionally tracked so others can clone & run.
*.test
bin/
dist/
build/
.build/
target/
.gradle/
coverage.out
coverage.html

# Logs and temporary files
/log/
*.log
*.bak
*~
tmp/
temp/

# Python
venv/
.venv/
__pycache__/
*.py[cod]
.pytest_cache/

# Go
vendor/

# Node / browser extension
node_modules/

# macOS / Windows / editors
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
